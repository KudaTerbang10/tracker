# domain
- Admin cabang can also print problematic transaction reports (gagal kirim and barang hilang) for their own branch — the print button should NOT be restricted to super_admin only. The report data should be scoped to the admin's cabang. Confidence: 0.85
- Use "Anulir Gagal Kirim" (not "Batalkan Gagal Kirim") for the gagal_kirim cancellation label — "Batalkan" implies canceling an order which is misleading; "Anulir" is the correct term for voiding/canceling the gagal_kirim status. Confidence: 0.70
- The `batalkan-hilang` route is restricted to Super Admin only (rbac('super_admin')), while `batalkan-gagal-kirim` can be performed by both Super Admin and Admin Cabang (rbac('super_admin', 'admin_cabang')). Confidence: 0.70
- When a Super Admin marks a "hilang" (lost) item as "selesai" (resolved/completed) on the Problematic Transactions page, that transaction should also appear in the "Selesai" (Completed) tab of the corresponding branch admin's Problematic Transactions page — the completion status must propagate to the branch level. Confidence: 0.70
- Multiple drivers can be assigned to the same package sequentially across different legs of its journey
- Multiple drivers can be assigned to the same package sequentially across different legs of its journey (e.g., Driver A from gudang A → gudang B, Driver B from gudang B → penerima). The top-level `driver_user_id` always reflects only the **current** assigned driver, while `tracking_logs[].driver_ditugaskan` stores the history of all drivers who handled it. Confidence: 0.80
- Users refer to the orange count badge on the "Perlu Dikirim" tab as the "task icon banner" — it shows the count of delivery tasks. Confidence: 0.90
- The `current_cabang_id` lifecycle during shipment tracking: `keluar_cabang`/`proses_kirim` → set to `null` (in-transit, no cabang has it); `diterima_cabang` → set to the receiving cabang's ID; `diterima` (final) → set to `null`. This ensures shipments appear only in the correct cabang's Current tab based on physical possession. Confidence: 0.75
- Track manifest number (`no_manifest`) changes in `tracking_logs` to maintain an auditable history of manifest assignments, similar to how driver assignment history is tracked via `tracking_logs[].driver_ditugaskan`. Confidence: 0.65
- For track resi: When a manifest number is assigned, insert it into the existing "keluar cabang" process/entry — do NOT create a separate/redundant "keluar cabang" entry just for the manifest number. The manifest number should be part of the existing shipment flow, not a standalone process. Confidence: 0.70
- For this Hira Express project: Expected data volume is max ~200 transactions per day per cabang and max ~200 deliveries per day per driver, so using `limit: 999` (or similar bulk fetch) on current/processing tabs is safe and performant. History tabs use date range filtering (not infinite scroll) to prevent loading all database data. Confidence: 0.65
- For the driver Riwayat (history) tab: Use infinite scroll with a limit of 20 items per page, as each driver's history is scoped to their own userId (unique Riverpod cache key) and won't conflict with other drivers' data. Confidence: 0.70
- For Super Admin: "gagal_kirim" (failed to send) items should NOT have a completion/resolution button — only "hilang" (lost) items get a completion button. Both "hilang" and "gagal_kirim" items should be filtered OUT of the main super admin transaction list and only appear on the "Barang Hilang / Terkendala" (Problematic Transactions) page — unless the item's status has been set to "ketemu" (found), in which case it should still appear in the main list. Confidence: 0.85
- "Scan Barang Diterima" is NOT the same as "Scan Barang Datang" (diterima_cabang). "Scan Barang Diterima" (`diterima` status) is for when the recipient/customer picks up their package directly at the warehouse/cabang — it finalizes the delivery to the end recipient. "Scan Barang Datang" (`diterima_cabang` status) is for when a shipment arrives at a branch from a driver/manifest. Confidence: 0.80
- For print/report functions: Pass the already-loaded data from the UI screen directly as a parameter instead of re-fetching from the API — the page already has the data, so avoid redundant network calls during printing. Confidence: 0.70
- For resolved/completed "gagal_kirim" transactions: Hide the "Batalkan Gagal Kirim" button — once a gagal kirim case is marked as selesai (resolved), the cancel action should no longer be available. Confidence: 0.70
- On the Bottom Sheet Tracking landing page: If the item status is "hilang" (lost) or "kasus_selesai" (case resolved), do NOT display the map. If the item status is "gagal_kirim" (failed delivery), display the map showing the item's current position — and if the item is still at a cabang (branch), use a warehouse marker icon instead of a truck icon. Confidence: 0.80
- For COD retur (gagal_kirim) transactions at the origin branch: Show them in payment management immediately when gagal_kirim is reported — include a StatusBadge so admin can distinguish physical status (gagal_kirim = still in transit, diterima_cabang = arrived at warehouse). Do NOT filter by status_saat_ini. Confidence: 0.80
- For tracking map display when status is `diterima_cabang` (received at branch): Show a single marker at the coordinates of the branch that performed the "scan barang datang" (the receiving branch), not at the origin/sender branch. The map should center on that receiving branch, similar to how `gagal_kirim` shows a single marker at the current cabang location. Confidence: 0.85
- For multi-hop shipment tracking (e.g., A → B → C): The route origin on the tracking map should be the last `diterima_cabang` entry in the tracking logs (the shipment's current physical position), not the first/original sender branch. The route destination should be derived from the last `keluar_cabang`/`proses_kirim` tracking log's `tujuan` field. This ensures the map shows the current leg of the journey instead of always showing the route from the very first branch. Confidence: 0.85
