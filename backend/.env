# === PILIH SALAH SATU URI (hapus # di depan) ===
# 1. MongoDB Atlas (production) â€” pastikan IP sudah diwhitelist
MONGO_URI=mongodb+srv://adiaz:100795@cluster0.w1nn1ua.mongodb.net/yulis_cargo?retryWrites=true&w=majority

# 2. Local MongoDB (development) â€” install MongoDB atau jalankan docker-compose.yml
# MONGO_URI=mongodb://localhost:27017/yulis_cargo

# 3. Local via Docker: docker-compose up -d  (PORT 27017)
#
# Setelah ganti URI, jalankan: npm run seed

JWT_SECRET=yulis_cargo_jwt_secret_2026
PORT=5000
