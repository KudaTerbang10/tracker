# === PILIH SALAH SATU URI (hapus # di depan) ===
# 1. MongoDB Atlas (production) — pastikan IP sudah diwhitelist
MONGO_URI=mongodb+srv://adiaz:100795@cluster0.w1nn1ua.mongodb.net/ekspedisi_tracker?retryWrites=true&w=majority

# 2. Local MongoDB (development) — install MongoDB atau jalankan docker-compose.yml
# MONGO_URI=mongodb://localhost:27017/ekspedisi_tracker

# 3. Local via Docker: docker-compose up -d  (PORT 27017)
#
# Setelah ganti URI, jalankan: npm run seed

JWT_SECRET=ekspedisi_tracker_jwt_secret_2026
PORT=5000
